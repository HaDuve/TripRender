# Copy to .env (never commit .env)

# Web build / Mapbox
MAPBOX_TOKEN=pk.your.mapbox.token

# App Store Connect API key (altool upload). Key file: ~/private_keys/AuthKey_<KEY_ID>.p8
# https://v2.tauri.app/distribute/app-store/#authentication
APPLE_API_KEY_ID=
APPLE_API_ISSUER=
